
#ifndef DIVIDIR_H
#define DIVIDIR_H

#include <QPixmap>
#include <QVector>

void dividir(const QString &spriteSheetPath, int spriteWidth, int spriteHeight, QVector<QPixmap> &sprites);

#endif // DIVIDIR_H
